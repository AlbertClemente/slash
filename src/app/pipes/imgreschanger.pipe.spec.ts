import { ImgreschangerPipe } from './imgreschanger.pipe';

describe('ImgreschangerPipe', () => {
  it('create an instance', () => {
    const pipe = new ImgreschangerPipe();
    expect(pipe).toBeTruthy();
  });
});
